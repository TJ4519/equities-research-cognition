# Current Semantic Checkpoint

Milestone 1 is complete, the independent verdict `ACCEPT_WITH_EXACT_NARROWING` is adopted, and three current-base workers are active under Level 2 bounded orchestration.

Status: `CONTINUING`.

Checkpoint date: 17 August 2026.

This is a translating projection. Current code and migrations govern what exists. Governing contracts, the accepted review narrowing, and `DECISION_LEDGER.jsonl` govern product and delivery authority.

## Exact repository basis

- Repository: `TJ4519/equities-research-cognition`
- Governing handoff branch: `agent/pro-responsibility-handoff`
- Verified handoff commit: `c1fc568424facbb5bb8e1b6369d30a1f380ae308`
- PRO branch: `agent/pro-grounding`
- Hostile review base: `263d7ad146a511482b6de1e0f3cd167b49593270`
- Hostile review result: `99ad9412f059239c0604497b3de868b6c348370a`
- Exact review verdict: `ACCEPT_WITH_EXACT_NARROWING`
- Authoritative product-code basis for current workers: `1798427cadad65e286e8e124b261dfb31903fec0`
- Packet-bundle commit used to create worker branches: `e2d476e3686b3d9723ef12d6241c6578a363a7ea`
- Dispatch receipt: `docs/pro/evidence/MILESTONE_2_IMPLEMENTATION_DISPATCH_RECEIPT.md`
- Ledger commit authorising the topology: `546dca9934565a80cdd13a541fbc03e108d86258`
- This checkpoint’s parent basis: `546dca9934565a80cdd13a541fbc03e108d86258`

No product code, migration, prompt, runtime, fixture, test, route or analyst-facing template changed during review adoption and packet compilation. The active worker branches begin at the packet bundle and must verify that the delta from the product-code basis contains only governing, checkpoint, ledger, evidence and packet metadata.

## Governing read order

A fresh owner, implementor or reviewer must:

1. verify repository, branch, commit, ancestry and divergence;
2. read root and package `AGENTS.md` and `ROUTE.md`;
3. read `PRO_RESPONSIBILITY_PROMPT.md`;
4. read `docs/pro/PRO_CONSTITUTION.md`;
5. read `docs/pro/END_TO_END_DELIVERY_COMMISSION.md`;
6. read `docs/pro/AUTONOMOUS_PRODUCT_OWNER_CHARTER.md` and Amendment 001;
7. read `docs/pro/PRODUCT_SYSTEM_ARCHITECTURE_V0.md`;
8. read `docs/pro/PROFESSIONAL_INTERACTION_AND_STATE_CONTRACT_V0.md`;
9. read `docs/pro/CONCEPTUAL_MODEL_CONTRACT_V0.md`;
10. read `docs/pro/EVALUATION_AND_HARNESS_EVOLUTION_CONTRACT_V0.md`;
11. read `docs/pro/UI_AND_REPOSITORY_SLOP_AUDIT.md`;
12. read `docs/pro/evidence/HOSTILE_VERTICAL_REVIEW_RECEIPT.md` in full;
13. read `docs/pro/CASE_A_OUTCOME_COMPLETE_VERTICAL_CONTRACT_V0.md`;
14. read the latest decision-ledger entries, counterexamples and this checkpoint;
15. inspect current code and migrations for the active surface; and
16. read only the worker packet assigned to the branch.

Historical Packets 002 and 003 are not instruction sources.

## Completion object

The active product writer must deliver one independently reviewable synthetic Case A path:

```text
authenticate
-> resume one synthetic Micron ResearchJob and immutable campaign-backed episode
-> inspect the exact starting workbook and bounded FY2025 revenue object
-> confirm or correct meaning
-> separately authorise the reported-value method and source rule
-> run one real NTM/Codex work unit over captured sources with search closed
-> have the equal-valued 8-K proposal blocked before mutation
-> understand that no workbook changed
-> choose “Create a candidate using the filed annual report”
-> receive an exact child workbook only after the 10-K replacement proposal passes
-> inspect the target delta, two recalculated consequences, engine identity, warnings and formula-error state
-> record SIMULATE_NAMED_USE, REJECT or REWORK
-> recover the same state and immutable history after restart
-> emit one exact CorrectionRecord seed without harness promotion
```

The autonomous delivery checkpoint remains `VERIFIED_V0_READY_FOR_COLD_TEST`, which requires later independent integration and product verification. It is not analyst validation, client readiness, production readiness or professional permission to rely.

## Exact narrowing that cannot be weakened

### Severity-one closures

- typed proposals refer only to host-issued episode, object, artifact, assertion, target and operation identities;
- `model_change_v0` runs with `closed_captured_sources` and no unconditional `--search`;
- deterministic admission precedes native-artifact mutation;
- candidate creation requires one exact current `PASS` decision under service and database guards;
- additive `ResearchJob` owns one immutable `ResearchCampaign` per episode while existing campaign finality remains intact;
- the bounded adapter exposes `inspect`, `apply`, `calculate` and `compare` behind one audited process boundary.

### Severity-two closures

- the first implementation is one joined Case A outcome, not disconnected capability slices;
- only the minimum accepted canonical population is added;
- the repair action is ordinary-language consequence, not analyst-authored source metadata;
- conceptual identity may exist without a cell binding and structural work safely refuses;
- new canonical state and candidate joins are append-only under ORM and direct SQL;
- V0 exposes only `SIMULATE_NAMED_USE`, `REJECT` and `REWORK`;
- clean fresh/upgrade migration evidence and classifier repair are required before final integration acceptance; and
- a newly compiled direct-Excel test is required before any native-Excel claim.

Any weakening requires another hostile review.

## Product centre and negative constraints

The mature direction remains a continuing company, security or mandate environment with specialised workspaces. The first workspace is the bounded model-change path.

Excel is an artifact-adapter question, not the product ontology. LibreOfficeDev is a pinned proxy, not Excel or the mature product.

The inherited Casebook, campaign/runtime controls, raw candidate JSON, source-ID entry, packet and trace identifiers, workbench names, multi-agent panes, pipeline stages, generic approval, dashboards, sidebars, card grids, activity feeds and provenance consoles remain excluded from primary product interaction.

The new route is additive:

`/jobs/<job_id>/model-change/<episode_id>/`

Legacy `/campaigns/` routes remain support surfaces.

## Current implementation truth

Implemented substrate:

- authentication and owner-scoped lookup;
- exact input bytes and digests;
- immutable run specifications, proposals, work orders, runtime events, artifacts and trace links;
- proposal disposition before execution;
- exact packet/cognition/input derivation;
- allowlisted NTM/Codex lifecycle;
- tracked dispatch and completion observation;
- sealed output and attestation;
- provisional candidate/refusal custody;
- restart retrieval; and
- post-custody Langfuse diagnostics.

Mechanically demonstrated outside the product path:

- one bounded OOXML target inspection and patch;
- formula, target, format and style-semantic preservation;
- LibreOfficeDev recalculation in one environment;
- two expected dependent changes;
- repeatable semantic output; and
- invalid-formula detection.

Not implemented at this checkpoint:

- continuing job plus campaign-backed model-change episode;
- product artifact manifest and bounded dependency closure;
- separate meaning/method authority;
- host-captured source versions and assertions;
- typed `model-change-proposal/v0` output;
- closed-source real NTM/Codex product path;
- deterministic source admission and paired target;
- candidate-after-pass native artifact creation;
- product calculation receipt and comparison;
- amendment, invalidation, synthetic disposition, restart projection and correction seed;
- the ordinary-language model-change route; or
- joined browser/live-path evidence.

## Active worker topology

### Packet 005 — primary product implementation

- Packet: `docs/pro/worker-packets/005_CASE_A_OUTCOME_COMPLETE_IMPLEMENTATION.md`
- Branch: `agent/case-a-outcome-complete-v0`
- Initial head: `e2d476e3686b3d9723ef12d6241c6578a363a7ea`
- GitHub issue: `#5`
- Authority: one architectural writer owns the complete joined Case A code, migrations, typed runtime envelope, deterministic gate, bounded adapter, ordinary-language route, tests and implementation receipt.
- Required return: `docs/pro/evidence/CASE_A_IMPLEMENTATION_RECEIPT.md` plus exact commit, tests, live NTM/Codex evidence, migration/rollback and nonclaims.

### Packet 006 — direct Excel reference evidence

- Packet: `docs/pro/worker-packets/006_DIRECT_EXCEL_REFERENCE_SURFACE_V0.md`
- Branch: `agent/direct-excel-reference-v0`
- Initial head: `e2d476e3686b3d9723ef12d6241c6578a363a7ea`
- GitHub issue: `#6`
- Authority: evidence-only operation of plainly disposable synthetic artifacts on the real authenticated Excel surface.
- Product authority: none. The result gates Excel/native-workflow claims and may not inspect personal workbooks or alter code.

### Packet 007 — classifier hygiene

- Packet: `docs/pro/worker-packets/007_BINARY_FIXTURE_CLASSIFIER_V0.md`
- Branch: `agent/binary-fixture-classifier-v0`
- Initial head: `e2d476e3686b3d9723ef12d6241c6578a363a7ea`
- GitHub issue: `#7`
- Authority: narrow explicit binary-fixture measurement with strict UTF-8 text and hostile fail-closed regressions.
- Product authority: none. The result is required before final integration acceptance.

The Excel and classifier workers own disjoint surfaces and may proceed in parallel. Packet 005 is the only product implementation authority.

## Closed and historical work

- Packet 001: closed workbook mechanism evidence.
- Packets 002 and 003: suspended, stale and not instruction sources.
- Packet 004: closed hostile-review evidence.
- GitHub issue `#4`: closed as completed architecture adjudication.
- `MU-MODEL-UPDATE-V0` and Amendment 001: historical proof, not active build order.

## Transfer constraints, not implementation scope

Case B must remain representable as a host-captured charge plus human-authored assumption, deterministic derivation, amendment and invalidation. It is not implemented as a product journey now.

Case C requires conceptual identity independent of a cell and safe `UNSUPPORTED_STRUCTURAL_OPERATION` refusal. No structural spreadsheet construction is authorised.

## Migration, runtime and rollback law

- migrations are additive after `campaign.0004`;
- no destructive backfill, rename or reinterpretation of legacy rows;
- existing campaigns remain legacy episodes;
- new V0 episodes create one new campaign;
- all new canonical facts are append-only under ORM and direct SQL;
- cross-record guards enforce owner/job/episode/campaign and candidate-after-pass closure;
- `MODEL_CHANGE_V0` defaults off outside isolated V0 scope;
- code rollback disables the flag and preserves V0 history;
- populated V0 schema remains dormant and forward-fixed rather than dropped;
- one real NTM/Codex worker executes Case A with no planner/reviewer/synthesis/judgment fan-out;
- only the audited host-process boundary may spawn the pinned calculation proxy; and
- the original workbook remains byte-for-byte unchanged.

## Human-only authority

Protected/client/personal data, personal workbooks, permanent credentials, API consent, account administration, material expenditure, analyst/client contact, external production release, investment/legal/publication judgment, professional permission to rely and production-tenant harness promotion remain human-only unless separately delegated.

A synthetic disposition proves state-transition mechanics only.

## Evidence ceiling

The repository now establishes:

- a credible custody kernel;
- bounded spreadsheet proxy mechanism evidence;
- an independently audited and exactly narrowed product contract;
- Level 2 authority;
- three current-base packets with disjoint ownership; and
- direct coordination through issues `#5`–`#7`.

It does not establish product implementation, direct Excel support, typical workbook support, analyst usefulness, professional correctness, permission to rely, harness improvement, deployment, verified V0 or client readiness.

This is not a product demo.

## Next action

The three workers execute their exact packets and return commits and receipts directly to PRO and coordinating Codex. PRO reconciles each return, integrates only evidence-compatible changes, and commissions fresh hostile verification of the joined candidate. No routine human decision is required.
