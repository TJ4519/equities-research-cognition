# Current Semantic Checkpoint

Packet 005 is rejected pending bounded repair: its real joined happy path is retained as historical mechanism evidence, but a forged database `PASS` can bypass the canonical application block and create an unauthorised candidate.

Status: `REPAIRING`.

Checkpoint date: 18 August 2026.

This is a translating projection. Executable code and migrations govern what exists. The product contracts, `DECISION_LEDGER.jsonl` through decision 023, `DECISION_LEDGER_CONTINUATION_2026-08-18.jsonl` beginning with decisions 024–025, and this checkpoint govern current repair authority.

## Exact repository and evidence basis

- Repository: `TJ4519/equities-research-cognition`
- Governing handoff branch: `agent/pro-responsibility-handoff`
- Verified handoff commit: `c1fc568424facbb5bb8e1b6369d30a1f380ae308`
- PRO branch: `agent/pro-grounding`
- Last accepted packet-dispatch head before the rejected return: `dee299679562db02f051997f244868e86d6e0a0e`
- Rejected implementation branch: `agent/case-a-outcome-complete-v0`
- Rejected implementation commit: `cf5685383654c00513825fc451b60221bc0117e9`
- Rejected evidence head: `3d5c3157f91361bee120c10ecd4bddfda6e16296`
- Coordinator projection: `dec-2026-08-18-037`
- Governing repair contract: `docs/pro/CASE_A_BOUNDED_REPAIR_CONTRACT_V1.md`
- Product repair branch: `agent/case-a-authority-recovery-repair-v1`
- Product repair packet head: `07db2a6b1f0e4b9f8aaca4f2bc5e5cb3192e1e43`
- Final classifier branch: `agent/classifier-final-surface-repair-v1`
- Final classifier packet head: `2c486b6d7a682be42cdf9c5758b4eb8e1d01055f`
- Product repair coordination: GitHub issue `#8`
- Classifier repair coordination: GitHub issue `#9`

No rejected Packet 005 product code has been merged into `agent/pro-grounding`. No deployment, direct-Excel operation, professional-use transition, or harness promotion is authorised by this checkpoint.

## Governing read order

A fresh owner, implementor, or verifier must:

1. verify repository, branch, commit, ancestry, and divergence;
2. read root and package `AGENTS.md` and `ROUTE.md`;
3. read `PRO_RESPONSIBILITY_PROMPT.md`;
4. read `docs/pro/PRO_CONSTITUTION.md`;
5. read `docs/pro/END_TO_END_DELIVERY_COMMISSION.md`;
6. read `docs/pro/AUTONOMOUS_PRODUCT_OWNER_CHARTER.md` and Amendment 001;
7. read the joined product, interaction, conceptual-model, evaluation, and slop contracts;
8. read `docs/pro/evidence/HOSTILE_VERTICAL_REVIEW_RECEIPT.md` and `docs/pro/CASE_A_OUTCOME_COMPLETE_VERTICAL_CONTRACT_V0.md`;
9. read the rejected Packet 005 implementation receipt only as evidence;
10. read `docs/pro/CASE_A_BOUNDED_REPAIR_CONTRACT_V1.md`;
11. read `DECISION_LEDGER.jsonl`, the dated continuation, and this checkpoint;
12. inspect current code, migrations, views, templates, runtime path, and tests for the active branch; and
13. read only Packet 008 or Packet 009 according to branch ownership.

Historical Packets 005, 006, and 007 are not active instructions.

## What the rejected branch demonstrated

The following remain bounded observations:

- one real NTM/Codex worker ran over captured sources with search closed;
- the application gate returned `BLOCK_WRONG_DOCUMENT_CLASS` for the equal-valued annual 8-K proposal;
- an attributed 10-K replacement proposal was created;
- the bounded LibreOfficeDev proxy recalculated a child workbook and two declared consequences;
- synthetic disposition, restart, and a correction seed were exercised;
- the focused model-change replay passed 25 tests;
- the owned-job/workbook replay passed 19 tests; and
- Django, migration-drift, boundary, and diff checks passed in the reported environment.

These facts justify a repair attempt. They do not establish candidate authority or a verified V0.

## Findings adopted

### P0 — database candidate-authority bypass

The PostgreSQL candidate trigger trusts stored `AdmissibilityDecision.outcome = PASS` plus structural joins. It does not recompute the target-specific source/target result or require the canonical validator and reason. A direct-SQL attacker can insert `validator=attacker/v0`, mark the annual 8-K proposal `PASS`, and insert a candidate even though the application gate correctly blocks it. A matching calculation receipt is not required at transaction completion.

### P1 — repeated repair

The same invalidated block may be repaired repeatedly, producing multiple amendment/replacement/pass/candidate chains.

### P1 — stranded calculation failure

Repair commits amendment, replacement, invalidation, and `PASS` before candidate creation. Adapter failure can escape as HTTP 500 and restart leaves a current `PASS`, no candidate, and no legal retry action.

### P1 — lost refusal

A bounded worker refusal is returned from the service but discarded by the view. It does not survive restart and exposes no next action.

### P1 — final classifier mismatch

At evidence head `3d5c3157...`, classification fails first on package-level `docs/pro/evidence/CASE_A_IMPLEMENTATION_RECEIPT.md`, before reaching the binary workbook. The accurate reproduced suite is 143 discovered, 138 executed, one setup error. Packet 007’s binary-only diagnosis is therefore superseded.

### P1 — incomplete candidate review

The candidate page omits the exact 10-K assertion and locator, original/candidate identities, named synthetic use, explicit unchanged-original fact, and the equal-value/annual-report explanation required by the vertical contract.

## Active repair completion object

Packet 008 must produce a reviewable repair candidate in which:

```text
PostgreSQL recomputes canonical target/source admission
-> forged decision and candidate inserts fail
-> candidate and exact CalculationReceipt are complete at transaction commit
-> filed-report repair is current, single-use, concurrent-safe and idempotent
-> adapter failure retains the original block and persists CALCULATION_FAILURE
-> runtime failure and worker refusal persist with an ordinary retry action
-> one fresh real NTM/Codex Case A episode succeeds after repair
-> candidate review shows exact source, artifact, use and unchanged-original evidence
-> restart and feature-flag rollback preserve append-only history
-> one correction seed is emitted without harness promotion
```

Packet 009 must make the final repository tree classify by adding strict UTF-8 package evidence support and narrow OOXML binary-fixture measurement without broad exception handling.

## Active worker topology

### Packet 008 — product authority and recovery repair

- Packet: `docs/pro/worker-packets/008_CASE_A_AUTHORITY_RECOVERY_REPAIR_V1.md`
- Branch: `agent/case-a-authority-recovery-repair-v1`
- Starting packet head: `07db2a6b1f0e4b9f8aaca4f2bc5e5cb3192e1e43`
- Issue: `#8`
- Product-code authority: yes, only for packet-owned repair files.
- Exact migration: `campaign.0007_model_change_v0_repair_1`.
- Required return: `docs/pro/evidence/CASE_A_REPAIR_1_RECEIPT.md` with `PASS_FOR_INDEPENDENT_VERIFICATION`, `PARTIAL`, or `FAIL`.

Packet 008 must first reproduce all six findings. It may not rewrite migrations 0005/0006, change the protocol or NTM process boundary, touch classifier code, widen the workbook profile, implement Cases B/C as journeys, merge, deploy, or update governing state.

### Packet 009 — final classifier surface repair

- Packet: `docs/pro/worker-packets/009_FINAL_CLASSIFIER_SURFACE_REPAIR_V1.md`
- Branch: `agent/classifier-final-surface-repair-v1`
- Starting packet head: `2c486b6d7a682be42cdf9c5758b4eb8e1d01055f`
- Issue: `#9`
- Product-code authority: none.
- Required return: `docs/pro/evidence/FINAL_CLASSIFIER_SURFACE_REPAIR_V1_RECEIPT.md` with `PASS`, `PARTIAL`, or `FAIL`.

Packet 009 owns only classifier support code, hostile classifier tests, and its receipt. It must handle the actual final evidence markdown and only the exact validated workbook fixture while preserving strict UTF-8 and unknown-binary rejection.

The two branches have disjoint ownership and may proceed in parallel. Packet 008 is the only active product-code authority.

## Closed, rejected, suspended, and superseded work

- Packet 001: closed historical workbook mechanism evidence.
- Packets 002 and 003: stale historical instructions.
- Packet 004: closed hostile architecture review evidence.
- Packet 005: rejected implementation evidence; issue `#5` closed `not_planned`.
- Packet 006: suspended; issue `#6` closed. No direct-Excel operation may advance.
- Packet 007: superseded by Packet 009; issue `#7` closed.
- `agent/case-a-outcome-complete-v0`: retained as rejected evidence only.
- `MU-MODEL-UPDATE-V0`: historical build evidence, not active sequence.

## Migration, retry, and rollback law

- repair migration is additive after rejected `campaign.0006`;
- migrations 0005 and 0006 remain immutable evidence;
- PostgreSQL recomputes admission for decision and candidate inserts;
- direct-SQL candidate without the exact matching receipt fails at commit;
- one successful repair chain exists per current block/action;
- runtime failure, refusal, and calculation failure are append-only canonical outcomes;
- restart projects one legal next action without live NTM, Langfuse, flash, or process memory;
- `MODEL_CHANGE_V0` rollback disables future entry while preserving all history; and
- populated repair schema remains dormant and is forward-fixed rather than dropped.

## Independent verification gate

Neither repair branch may merge itself or update `agent/pro-grounding`.

After both returns, PRO must inspect exact ancestry, changed-file ownership, diffs, receipts, migrations, direct-SQL counterexamples, full test counts, and one fresh joined episode. A fresh independent verifier must then reproduce:

- forged PASS/candidate rejection;
- repeat/concurrent repair convergence;
- calculation failure, restart, and retry;
- persisted refusal and runtime failure;
- candidate-review evidence;
- classifier fail-closed cases;
- restart and rollback; and
- the new real joined Case A episode.

Only that later adjudication may integrate a repaired candidate or restore a verified-V0 claim.

## Human-only authority

Protected, client, analyst, or personal data; personal workbooks; permanent credentials; API consent; account administration; material expenditure; analyst/client contact; external production release; investment/legal/publication judgment; professional permission to rely; and production-tenant harness promotion remain human-only unless separately delegated.

## Evidence ceiling

The repository now has:

- a rejected but materially informative joined implementation branch;
- independently reproduced P0/P1 defects;
- a governing bounded repair contract;
- two disjoint current-head repair packets; and
- direct coordination through issues `#8` and `#9`.

It does not establish a repaired product, verified V0, analyst usefulness, professional correctness, permission to rely, direct Excel support, arbitrary workbook support, harness improvement, deployment, or client readiness.

This is not a product demo.

## Next action

The Packet 008 and Packet 009 workers execute from their exact heads and return commits and receipts directly to PRO and coordinating Codex. Direct Excel remains suspended. No routine human decision is required at this gate.
