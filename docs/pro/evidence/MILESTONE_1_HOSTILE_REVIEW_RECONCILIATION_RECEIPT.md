# Milestone 1 Hostile Review Reconciliation Receipt

Verdict: `PASS` for architecture adjudication and transition to Level 2 bounded vertical orchestration.

Recorded: 17 August 2026.

## Exact basis

- Repository: `TJ4519/equities-research-cognition`
- Review branch: `agent/hostile-vertical-review`
- Review base: `263d7ad146a511482b6de1e0f3cd167b49593270`
- Review result: `99ad9412f059239c0604497b3de868b6c348370a`
- Only review change: `docs/pro/evidence/HOSTILE_VERTICAL_REVIEW_RECEIPT.md`
- Exact independent verdict: `ACCEPT_WITH_EXACT_NARROWING`
- PRO adoption contract: `docs/pro/CASE_A_OUTCOME_COMPLETE_VERTICAL_CONTRACT_V0.md`
- PRO adoption commit: `5f27751a4c3863130f1b8650f76b2f4bc34b4409`

The review result was fast-forwarded into `agent/pro-grounding` before adoption. The reviewer changed no product code, migrations, routes, prompts, tests, governing contracts, or worker packets.

## Evidence independently supplied by the coordinator

The coordinating agent independently verified:

- result ancestry from the exact review base;
- allowed-file scope;
- Django system check;
- migration drift check;
- package boundary check;
- diff check;
- focused owned-job/workbook suite with `19/19` passing; and
- reproduction of the known `tools/classify_loc.py` UTF-8 failure on the binary `.xlsx` fixture.

This coordinator evidence is accepted as independent corroboration of the review receipt and its claim ceiling. It is not product implementation evidence.

## PRO adjudication

PRO adopts the review's exact narrowing without removing or weakening any S1/S2 closure.

The accepted implementation unit is one joined synthetic Micron Case A episode that:

1. begins or resumes one continuing `ResearchJob` with one immutable campaign-backed episode;
2. inspects and human-confirms one bounded FY2025 revenue object;
3. separately authorises its reported-value method and target-specific source policy;
4. runs one real NTM/Codex worker over exact captured sources with search closed;
5. receives the controlled equal-value 8-K proposal;
6. returns `BLOCK_WRONG_DOCUMENT_CLASS` before candidate creation;
7. records the attributed action `Create a candidate using the filed annual report`;
8. creates a replacement proposal bound to the captured 10-K;
9. requires deterministic `PASS` before creating an exact child workbook;
10. patches and calculates through the bounded proxy profile with exact ancestry, engine identity, two consequences, and formula-error status;
11. records only `SIMULATE_NAMED_USE`, `REJECT`, or `REWORK`;
12. reconstructs state after restart from append-only canonical records; and
13. emits one exact `CorrectionRecord` seed without harness promotion.

A paired preliminary earnings-flash target must permit the same 8-K assertion. Cases B and C remain transfer tests for representation, invalidation, independent conceptual identity, and safe structural refusal; they are not first-vertical implementation scope.

## Mandatory closures adopted

### Severity one

- typed host-issued proposal, object, artifact, and source-assertion references;
- closed captured-source policy and absence of unconditional `--search` for `model_change_v0`;
- deterministic source-to-target admission;
- candidate-after-pass database and service enforcement;
- additive `ResearchJob` above one immutable `ResearchCampaign` per episode;
- stable `inspect/apply/calculate/compare` adapter boundary; and
- one audited process boundary for the pinned calculation engine.

### Severity two

- one joined Case A implementation unit rather than separate A–D delivery slices;
- minimum canonical record population only;
- consequence-language source repair owned by the host;
- conceptual-object identity independent of a cell address and safe `UNSUPPORTED_STRUCTURAL_OPERATION` refusal;
- direct-SQL append-only and cross-record guards;
- synthetic-use-only terminology and scope;
- fresh and upgrade migration evidence;
- binary-fixture classifier repair before final integration acceptance; and
- a newly compiled current-base direct-Excel test before any native-Excel claim.

## Programme transition

Milestone 1 is complete. Worker Packet 004 is closed as historical proof.

The programme moves to `CONTINUING` at Level 2 bounded vertical orchestration. The active implementation topology is authorised to contain exactly:

- one architectural writer for the joined Case A product vertical;
- one evidence-only direct-Excel reference-surface worker; and
- one narrow classifier-hygiene worker.

The latter two own disjoint files and may run in parallel. Neither may redefine the product or widen the primary implementation.

## Human-only boundaries

No new authority is granted over protected data, personal workbooks, permanent credentials, API consent, material expenditure, analyst/client contact, external production release, investment or professional judgment, professional permission to rely, or production-tenant harness promotion.

The V0 synthetic disposition proves state-transition mechanics only.

## Claim ceiling

This receipt establishes that the hostile-review gate closed with an accepted exact narrowing and that Level 2 bounded orchestration may begin under a fresh implementation contract. It does not establish product implementation, analyst usefulness, professional correctness, Excel parity, harness improvement, deployment, production readiness, or client readiness.
