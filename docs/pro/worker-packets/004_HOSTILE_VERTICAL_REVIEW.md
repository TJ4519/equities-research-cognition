# Worker Packet 004 — Independent Hostile Vertical Review

Status: active Milestone 1 review packet.

Owner: one fresh independent Codex reviewer. The reviewer is not the architectural writer and may modify only the receipt surfaces named below.

Governing commission: `docs/pro/END_TO_END_DELIVERY_COMMISSION.md`.

Review contract: `docs/pro/HOSTILE_VERTICAL_REVIEW_CONTRACT_V0.md`.

## Professional outcome

Determine whether the joined product-system contracts can be narrowed into one implementable, outcome-complete model-change vertical that completes a recognisable analyst job from bounded understanding through controlled failure, correction, recalculated candidate, scoped synthetic use mechanics, restart and evaluation seed.

The worker must return an exact verdict that lets the PRO product owner accept, repair or kill the vertical and immediately compile the next lawful implementation packet.

## Exact base

Repository: `TJ4519/equities-research-cognition`.

Branch: `agent/hostile-vertical-review`.

The worker must verify that its branch descends from the packet commit named in the branch creation or coordinating issue. If the branch moved, stop and return `STALE_BASE` rather than reviewing mixed authority.

## Required read order

1. root `AGENTS.md` and `ROUTE.md`;
2. package `AGENTS.md` and `ROUTE.md`;
3. `PRO_RESPONSIBILITY_PROMPT.md`;
4. `docs/pro/PRO_CONSTITUTION.md`;
5. `docs/pro/END_TO_END_DELIVERY_COMMISSION.md`;
6. `docs/pro/AUTONOMOUS_PRODUCT_OWNER_CHARTER.md`;
7. `docs/pro/AUTONOMOUS_PRODUCT_OWNER_CHARTER_AMENDMENT_001.md`;
8. `docs/pro/PRODUCT_SYSTEM_ARCHITECTURE_V0.md`;
9. `docs/pro/PROFESSIONAL_INTERACTION_AND_STATE_CONTRACT_V0.md`;
10. `docs/pro/CONCEPTUAL_MODEL_CONTRACT_V0.md`;
11. `docs/pro/EVALUATION_AND_HARNESS_EVOLUTION_CONTRACT_V0.md`;
12. `docs/pro/UI_AND_REPOSITORY_SLOP_AUDIT.md`;
13. `docs/pro/HOSTILE_VERTICAL_REVIEW_CONTRACT_V0.md`;
14. `docs/pro/COUNTEREXAMPLE_REGISTER.md`;
15. `docs/pro/REPOSITORY_GROUND.md`;
16. `docs/pro/PROMPT_AND_HARNESS_AUDIT.md`;
17. `docs/pro/evidence/WORKBOOK_CAPABILITY_SPIKE_RECEIPT.md`;
18. `docs/pro/DECISION_LEDGER.jsonl`;
19. `docs/pro/CURRENT_SEMANTIC_CHECKPOINT.md`; and
20. current code, migrations, routes, templates, tests and runtime compilers required by the review.

Repository code and migrations govern what exists. Governing product contracts govern what should be built. Translating files may be stale and must be challenged.

## Owned files

The worker may create or modify only:

- `docs/pro/evidence/HOSTILE_VERTICAL_REVIEW_RECEIPT.md`; and
- optional sanitised files under `docs/pro/evidence/hostile-vertical-review/`.

No governing document, code, migration, prompt, skill, route, template, test, packet or existing receipt may change.

## Required work

Execute the complete review in `HOSTILE_VERTICAL_REVIEW_CONTRACT_V0.md`.

At minimum:

1. inspect current models, migrations, services, routes, templates and tests rather than relying on `REPOSITORY_GROUND.md` alone;
2. instantiate materially different synthetic Case A, B and C reasoning traces;
3. trace every proposed visible action to canonical state, validator, artifact consequence, correction, invalidation, evaluation signal and support status;
4. identify canonical objects that can be deferred or projected in V0;
5. identify the minimum additive migration boundary and rollback;
6. inspect NTM/Codex dispatch and source-search behaviour;
7. inspect the existing workbook probe and historical receipt;
8. determine whether an updated direct-Excel assumption test is a precondition, parallel dependency or later adapter test;
9. calculate the analyst confirmation and review burden qualitatively and, where possible, as explicit interaction counts;
10. attack restart, stale packet, stale descendant, use-scope and rollback semantics;
11. return severity-one and severity-two defects; and
12. select exactly one verdict.

## Independence and evidence

The worker must state that it had no prior hidden chat context and list every repository surface independently inspected. It must not claim technical isolation that it cannot prove.

Every finding must cite exact repository paths, current code or governing sections. A recommendation unsupported by an exact object, interaction, test or migration consequence is not a finding.

## Acceptance standard

`ACCEPT_WITH_EXACT_NARROWING` is allowed only when the worker can name:

- the first vertical's exact visible journey;
- the minimum canonical object population;
- the minimum additive migration boundary;
- stable service and adapter interfaces;
- exact deterministic gates and refusal states;
- exact artifact and correction consequences;
- restart and rollback;
- required hostile tests and live evidence;
- product-form assumptions retained only as falsifiable hypotheses; and
- no unresolved severity-one or severity-two architecture defect that blocks implementation.

`REVISE_AND_REVIEW_AGAIN` requires a bounded repair list and exact second-review question.

`KILL_OR_REPLACE_CURRENT_VERTICAL` requires the strongest rival and a reversible test.

## Stop conditions

Stop and return a bounded blocker when:

- the branch does not match the named base;
- required governing files are missing or contradictory in a way the reviewer cannot resolve without becoming the product owner;
- code required to verify a load-bearing claim is inaccessible;
- the review would require protected or private material;
- a current packet has been activated concurrently and creates conflicting authority; or
- the only way to accept the vertical is to invent analyst evidence.

Do not ask the user to resolve routine repository or conceptual ambiguity. Record the conflict for PRO adjudication.

## Required checks

The review is documentation-only, but the worker must use current executable surfaces to substantiate migration and runtime claims. At minimum:

- inspect current Django model and migration graph;
- inspect exact route and template population;
- inspect NTM/Codex launch, send, collect and trace lifecycle;
- inspect current adversarial tests for wrong-source, restart and custody semantics;
- inspect workbook capability code and receipt;
- run repository classification or syntax checks only when the environment is available and the known binary-fixture defect does not make the result misleading; and
- record every command and environmental limitation.

A green test suite is not required for a review verdict, but unsupported claims about current behaviour are prohibited.

## Receipt format

The receipt must include:

```text
VERDICT
BASE AND INDEPENDENCE
EXECUTIVE DEFECTS
CASE A — REPORTED UPDATE
CASE B — ASSUMPTION OR ADJUSTMENT
CASE C — STRUCTURAL ADDITION
MIGRATION AND CURRENT-CODE BOUNDARY
RUNTIME AND COGNITION
ADAPTER DECISION
INTERACTION AND REVIEW BURDEN
RESTART, INVALIDATION AND ROLLBACK
SMALLEST ACCEPTED OR RIVAL VERTICAL
STABLE INTERFACES
REQUIRED TESTS AND LIVE EVIDENCE
SEVERITY-ONE AND SEVERITY-TWO FINDINGS
CLAIM CEILING AND NONCLAIMS
RECOMMENDED NEXT PRO DECISION
```

## Required return

Return directly to the PRO and coordinating Codex context:

- exact base and result commit;
- changed-file list;
- primary verdict;
- severity-one and severity-two findings;
- smallest accepted or rival vertical;
- migration and rollback boundary;
- whether a direct-Excel test is required and why;
- exact implementation prerequisites;
- receipt path;
- commands and environment limitations; and
- claim ceiling.

Do not implement, merge, activate another worker, edit the product contracts, or claim that the product is ready to build. The PRO owns reconciliation and the next packet.